name: inventur_helper
description: Inventur Helper
publish_to: 'none'
version: 1.2.0

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_zxing:
  bits:
  barcode_widget:
  shared_preferences:
  path_provider:
  path:
  file_picker: ^11.0.2
  csv:
  collection:
  screen_brightness:
  app_links:
  share_plus:
  upgrader:
  version:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  package_info_plus:
  http:

flutter:
  uses-material-design: true
